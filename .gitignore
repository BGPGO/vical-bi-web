# Credenciais
.env
.env.local

# Build artifacts (tracked pra deploy Coolify)
data-extras/

# Reports cacheados (gerados por write-reports.cjs do cliente, ou por API on-the-fly)
# report.json
# report-*.json

# bi.config.js é cliente-específico mas NÃO tem secrets — é tracked pra cada repo cliente.
# Mantém aqui só pra referência (descomente se quiser experimentar local sem commitar).
# bi.config.js

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
*.log

# Editor
.vscode/
.idea/

# Não ignora QUICK_START.pdf — ele é distribuído junto pra time não-dev
# QUICK_START.pdf
